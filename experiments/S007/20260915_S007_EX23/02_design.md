# S007 EX23 设计

- 复用EX22冻结的完整搜索ledger，不读取新收益、不追加搜索；
- 精确参数去重后，在九维归一化参数空间内按L∞距离不超过0.20建立合格点邻接图；
- 平台沿用既有标准：至少10种独立合格交易行为，最大连通区域至少覆盖5种行为；
- 对最大平台逐参数检查上下5%边界带；任一参数有至少50%的参数点集中在同一侧边界，标记
  为边界依赖，平台只能进入边界扩展评审；
- 使用全部已完成且参数唯一的试验，对每个合格参数点计算最近20点的可行率和邻域半径，
  作为局部稳定性诊断，不追加事后硬门；
- 本轮不选择平台中心、不创建候选、不修改SM或PTE。
