# 0901_EX06 原子CZSC结构准入研究设计

从冻结信号清单中选择`atomic_structure`的40个信号，在三个频率分别以`di=1`生成。每个输出拆成`v1`、`v2`、`v3`和`joint`互斥状态；完全相同的二值路径保留一个规范因子并记录全部别名。

候选统一使用0901_EX05协议中的状态支持度、四年同向、0.5个百分点最小效应、年度影响份额、条件增量、冗余、因果前缀和可执行性门槛。主端点固定为20日收益与20日路径最大回撤，5日和60日只报告。

无论PASS或FAIL均归档候选覆盖矩阵、准入漏斗和全部审计；不得根据结果修改信号、频率、表示或门槛。

