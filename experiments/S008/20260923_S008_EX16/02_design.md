# S008 EX16 实验设计

EX16固定复用EX14协议和特征实现，并继承EX15对月度M+2开发截止边界的唯一技术修正。DFLS来源
身份更新到提交`1e30845e`；SHIBOR必须通过分年获取覆盖2013-07-29至2024-12-31。

本实验不增加、删除或改写特征，不改变覆盖率门槛，不生成未来收益标签，不筛选输入，不选择策略
原型。全部计算、身份、因果和覆盖检查通过时才裁决`PROCEED_TO_INFORMATION_VALUE_AUDIT`。
