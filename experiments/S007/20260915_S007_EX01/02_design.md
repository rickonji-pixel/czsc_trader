# S007 EX01 实验设计

1. 固定开发池、S001-v2风险收益标杆、证据速度目标及BuyHold对照；
2. 校验FSC与STC当前目录身份，但不选择输入和模板；
3. 按资产收益驱动登记五类可竞争、可证伪的假设及其最低数据需求；
4. 明确S007可继承和禁止继承的内容；
5. 输出完整假设账本，所有路线在下一轮接受相同的数据和因果门。

本轮不物化特征、不读取标签或收益、不搜索参数、不创建候选，不修改SM或PTE。
