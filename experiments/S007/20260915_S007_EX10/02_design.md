# S007 EX10 设计

- 先按交易行为哈希去重，避免多个数值参数产生同一交易路径而虚增平台宽度；
- 参数按原搜索边界归一化，L∞距离不超过0.20的试验相连；
- 平台要求至少10种合格交易行为，且同一连通区域至少覆盖5种交易行为；
- 对每种行为独立计算2021—2026各年绩效，并报告发现期、确认期与完整期收益平衡比；
- 时间集中度是EX09结果触发的事后诊断，只作风险披露和机制研究路由，不追溯修改搜索硬门；
- 不选择参数冠军、不创建候选、不调用SE冻结体检。

