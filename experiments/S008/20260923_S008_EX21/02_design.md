# S008 EX21 实验设计

1. EX20的`EXECUTION_ERROR`保持不变，不回写或覆盖；
2. 逐字复用EX20的`S008Prototype`源码，源码闭包身份必须相同；
3. 逐项继承EX20协议，仅将实验ID改为EX21并登记前序失败身份；
4. 唯一技术变化是使用`.tmp/S008_EX21_runtime`普通目录承载SRT隔离数据，并在执行结束后清理；
5. 其余执行、检查、输出和裁决逻辑复用EX20预注册脚本，不得修改。

