# S004 EX36 设计

- 只调用Tushare `margin_detail`并检查数据，不读取事件后收益；
- 固定使用融资买入额`rzmre`和融资偿还额`rzche`构造当日净融资流，不以融资余额日差替代；
- 历史研究只允许使用交易日T的数据生成T+1及以后信号；
- 历史数据门要求核心字段无缺失、无重复，从首条记录起覆盖率不低于98%，且不少于1,200日；
- Tushare文档所述约8:30更新不等于本地前瞻到达已验证，部署前仍需独立审计；
- 本轮不生成事件、不读取条件收益、不创建候选、不修改SM/PTE。

