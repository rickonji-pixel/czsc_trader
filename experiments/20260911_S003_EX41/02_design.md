# S003 EX41 设计

份额流率定义为`当日份额/前一交易日份额-1`，拆分日不计算。折溢价沿用公司行为治理后的
`收盘价/单位净值-1`。只报告方向、零值比例、分位数、滚动60日密度和两者同期相关性。

事件定义按固定顺序路由：若全部净赎回日已满足滚动60日中位数12—20且第10百分位不少于
8，采用`ALL_NET_REDEMPTION`；否则检查相对此前60日最低20%份额流；两者均不满足则停止。
该路由只使用事件数量，不读取任何事件后收益。

份额发布时间没有时分字段。后续事件统一在记录日后的首个交易日盘后确认，并最早于再下
一个交易日开盘执行，即保守的T+2执行。
