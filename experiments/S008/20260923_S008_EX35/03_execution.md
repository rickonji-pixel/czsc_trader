# S008 EX35 执行记录

状态：`EXECUTION_ERROR`。DFLS准备、13组件 EX16 逐值等价及完整 SRT 信号生成均成功，上市前
空仓边界生效。加速信号与完整 SRT 的逐列比较开始前，比较器要求加速历史提供完整 SRT 证据列
`prototype_id`，但加速函数仅调用原型内部决策函数，未补回该冻结标识，因而触发 `KeyError`。

错误发生在信号值比较、BuyHold和Optuna之前。没有计算目标收益；三套原型均为0/1,500 trial，
8逻辑核心搜索未启动。
