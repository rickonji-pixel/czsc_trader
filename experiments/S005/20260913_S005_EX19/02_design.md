# S005 EX19 设计

不重新请求数据、不改写EX18。固定校验EX18档案、质量报告和成分面板哈希，完整沿用其数据质量
门，只将一次性历史回填调用数改为披露项。OPC维护成本按日常增量评估：普通交易日分别按交易日
一次获取全市场`daily`与`moneyflow`；需要更新月度权重时再调用一次`index_weight`，单日最多3次。

如果数据质量全部满足且日常增量不超过3次，允许进入成分机制预注册。该纠偏不构成收益证据。

