# S003 EX29 设计

EX28的原始数据不变。修正两项与研究目标不一致的门槛：

1. 月度权重只要求覆盖截止月之前的全部完整自然月；未结束月份允许沿用最近一次已发布快照，
   但快照距截止日不得超过31天；
2. 历史成分并集与一次性回填调用量只报告，不作为持续运行门槛。OPC成本门关注完整压缩存储
   不超过200MB、日常增量日线调用不超过2次、成分权重每月更新。

其余质量要求保持：每快照500只、权重合计99.5至100.5、历史成分确有变化、抽样缺失均由
全日停牌解释。若通过，只允许建设完整宽度数据集，仍不得读取510500条件收益。
