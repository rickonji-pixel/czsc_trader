export function parseRoute(pathname) {
  const account = pathname.match(/^\/accounts\/([^/]+)$/);
  if (account) return {page: 'account', accountId: decodeURIComponent(account[1])};
  if (pathname === '/channels/futu') return {page: 'channel', channel: 'futu'};
  if (pathname === '/comparison') return {page: 'comparison'};
  return {page: 'account', accountId: null};
}

export class ScopedLoader {
  constructor(){this.sequence=0;this.scope=null;this.controller=null;}
  begin(scope){if(this.controller)this.controller.abort();this.controller=new AbortController();this.scope=scope;return {sequence:++this.sequence,scope,signal:this.controller.signal};}
  accept(request,scope){return request.sequence===this.sequence&&request.scope===scope;}
}

export const actionsForRoute = route => route.page==='channel'?['pause','resume','cancel']:route.page==='account'?['pause','resume']:[];
export const comparisonQuery = ids => ids.map(id=>`account_id=${encodeURIComponent(id)}`).join('&');

const esc = value => String(value??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money = value => value==null?'—':Number(value).toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2});
const pct = value => value==null?'—':`${(Number(value)*100).toFixed(2)}%`;
const shortHash = value => value?String(value).slice(0,10):'—';
const actionLabel = value => ({BUY:'买入',SELL:'卖出',WAIT:'等待'}[value]||value||'等待');
const statusLabel = value => ({READY:'就绪',OK:'正常',BLOCKED:'阻塞',SUBMITTED:'已提交',FILLED_ALL:'全部成交',PENDING:'待执行'}[value]||value||'—');
const state = {accounts:[],loader:new ScopedLoader(),lastSuccess:null};

async function getJson(url, options={}) {
  const response=await fetch(url,options);let payload={};
  try{payload=await response.json();}catch{payload={error:`HTTP ${response.status}`};}
  if(!response.ok)throw new Error(payload.error||`HTTP ${response.status}`);return payload;
}
function post(url,body={}){return getJson(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});}
function toast(message){const el=document.querySelector('#toast');el.textContent=message;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2200);}
function setNav(page){document.querySelectorAll('[data-nav]').forEach(el=>el.classList.toggle('active',el.dataset.nav===page));}

async function refreshSystem(){try{const s=await getJson('/api/system/status');const failures=s.scheduler_failures?.length||0;document.querySelector('#systemStatus').textContent=`PTE ${statusLabel(s.runtime)} · Futu ${statusLabel(s.futu_connection)} · 全局告警 ${s.alerts?.length||0}${failures?` · 失败任务 ${failures}`:''}`;}catch(error){document.querySelector('#systemStatus').textContent=`系统状态不可用 · ${error.message}`;}}
function accountList(active){return `<aside class="sidebar"><h2>虚拟账户</h2>${state.accounts.map(a=>`<button class="account-card ${a.account_id===active?'active':''}" data-account="${esc(a.account_id)}"><strong>${esc(a.name)}</strong><small>${esc(a.account_id)} · ${esc(a.release_id)}</small><small>${a.paused?'已暂停':statusLabel(a.health)} · 资产 ${money(a.total_assets)}</small></button>`).join('')}</aside>`;}
function metric(label,value){return `<div class="panel metric"><label>${label}</label><strong>${value}</strong></div>`;}
function rowsTable(columns,rows){if(!rows?.length)return '<div class="empty">暂无记录</div>';return `<div class="table-wrap"><table><thead><tr>${columns.map(c=>`<th>${c[0]}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${columns.map(c=>`<td>${esc(c[1](row))}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}
function alerts(items){return (items||[]).map(item=>`<div class="alert">${esc(item.message||item)}</div>`).join('');}

function renderAccount(snapshot){const a=snapshot.account,m=snapshot.metrics||{},d=snapshot.decision||{},scope=snapshot.scope;const orders=snapshot.orders||[],fills=snapshot.fills||[];document.querySelector('#app').innerHTML=`<div class="layout">${accountList(scope.account_id)}<div class="workspace"><section class="panel hero"><div><div class="eyebrow">虚拟账户 · ${esc(scope.account_id)}</div><h1>${esc(a.name)}</h1><div class="subtle">${esc(a.strategy_name_snapshot)} · ${esc(scope.release_id)} · ${shortHash(scope.release_hash)} · ${esc(a.qualification_snapshot)}</div><div class="subtle">观察起点 ${esc(a.observation_start)} · 最近同步 ${esc(snapshot.as_of)}</div></div><button id="accountSwitch" class="switch ${a.paused?'paused':''}" role="switch" aria-checked="${!a.paused}">${a.paused?'已暂停 · 点击恢复':'自动运行 · 点击暂停'}</button></section>${alerts(snapshot.alerts)}<div class="grid">${metric('总资产',money(a.total_assets))}${metric('可用现金',money(a.cash))}${metric('持仓',`${Number(a.quantity||0).toLocaleString()} 股`)}${metric('累计收益',pct(m.total_return))}</div><section class="panel section"><div class="section-head"><h2>最新策略决策</h2><span class="badge">${esc(scope.account_id)}</span></div><div class="decision"><div class="decision-action">${esc(actionLabel(d.action))}</div><div class="kv"><div><span>决策ID</span>${esc(d.decision_id)}</div><div><span>信号日期</span>${esc(d.signal_date)}</div><div><span>有效交易日</span>${esc(d.valid_session)}</div><div><span>目标持仓</span>${esc(d.target_quantity)}</div><div><span>执行参考价</span>${esc(d.execution_reference_price)}</div><div><span>未下单原因</span>${esc((snapshot.alerts||[])[0]?.message||'—')}</div></div></div></section><section class="panel section"><div class="section-head"><h2>模型订单</h2><span class="badge">账户 ${esc(scope.account_id)}</span></div>${rowsTable([['账户',r=>r.account_id],['订单',r=>r.order_id],['方向',r=>r.side],['数量',r=>r.quantity],['限价',r=>r.limit_price],['状态',r=>statusLabel(r.status)]],orders)}</section><section class="panel section"><div class="section-head"><h2>模型成交</h2><span class="badge">账户 ${esc(scope.account_id)}</span></div>${rowsTable([['账户',r=>r.account_id],['成交',r=>r.fill_id],['日期',r=>r.session],['方向',r=>r.side],['数量',r=>r.quantity],['价格',r=>r.price]],fills)}</section><section class="panel section"><div class="section-head"><h2>绩效与审计</h2><span class="badge">共同口径</span></div><div class="grid">${metric('最大回撤',pct(m.maximum_drawdown))}${metric('卡玛比率',m.calmar_ratio==null?'不可用':Number(m.calmar_ratio).toFixed(3))}${metric('盈亏比',m.win_loss_ratio==null?'不可用':Number(m.win_loss_ratio).toFixed(3))}${metric('已闭合交易',esc(m.closed_trades||0))}</div>${rowsTable([['时间',r=>r.created_at],['事件',r=>r.event_type],['内容',r=>JSON.stringify(r.payload)]],snapshot.events)}</section></div></div>`;bindAccountEvents(scope.account_id,a.paused);}
function bindAccountEvents(accountId,paused){document.querySelectorAll('[data-account]').forEach(el=>el.onclick=()=>navigate(`/accounts/${encodeURIComponent(el.dataset.account)}`));document.querySelector('#accountSwitch').onclick=async()=>{try{await post(`/api/virtual-accounts/${encodeURIComponent(accountId)}/${paused?'resume':'pause'}`);toast(paused?'账户已恢复':'账户已暂停');await loadRoute();}catch(e){toast(e.message);}};}

function renderChannel(s){const b=s.binding||{},d=s.decision||{};document.querySelector('#app').innerHTML=`<div class="workspace"><section class="panel hero"><div><div class="eyebrow">执行渠道 · FUTU</div><h1>Futu 模拟账户</h1><div class="subtle">${esc(s.scope.account_type)} · 行情 ${statusLabel(s.quote_health)} · 最近同步 ${esc(s.as_of)}</div></div><button id="channelSwitch" class="switch ${s.paused?'paused':''}" role="switch" aria-checked="${!s.paused}">${s.paused?'渠道已暂停 · 点击恢复':'渠道自动运行 · 点击暂停'}</button></section>${alerts((s.alerts||[]).map(x=>({message:x})))}<div class="grid">${metric('可用现金',money(s.account?.cash))}${metric('总资产',money(s.account?.total_assets))}${metric('实际持仓',`${Number(s.actual_quantity||0).toLocaleString()} 股`)}${metric('连接状态',s.connection_error?'不可用':'已连接')}</div><section class="panel section"><div class="section-head"><h2>当前执行绑定</h2><span class="badge">显式绑定</span></div><div class="kv"><div><span>策略发布</span>${esc(b.release_id||'未绑定')}</div><div><span>发布哈希</span>${esc(shortHash(b.release_hash))}</div><div><span>绑定时间</span>${esc(b.bound_at)}</div><div><span>绑定人</span>${esc(b.bound_by)}</div><div><span>绑定原因</span>${esc(b.reason)}</div><div><span>决策身份</span>${esc(d.strategy?.release_id)}</div></div></section><section class="panel section"><div class="section-head"><h2>最新渠道决策</h2><span class="badge">Futu</span></div><div class="decision"><div class="decision-action">${esc(actionLabel(d.action))}</div><div class="kv"><div><span>决策ID</span>${esc(d.decision_id)}</div><div><span>信号日期</span>${esc(d.signal_date)}</div><div><span>有效交易日</span>${esc(d.valid_session)}</div></div></div></section><section class="panel section"><div class="section-head"><h2>渠道订单与撤单</h2><span class="badge">仅Futu订单可撤</span></div>${rowsTable([['订单',r=>r.channel_order_id],['方向',r=>r.side],['数量',r=>r.quantity],['状态',r=>statusLabel(r.status)],['操作',r=>['SUBMITTED','PARTIAL_FILLED'].includes(r.status)?'二次确认撤单':'—']],s.orders)}</section></div>`;document.querySelector('#channelSwitch').onclick=async()=>{try{await post(`/api/channels/futu/${s.paused?'resume':'pause'}`);toast(s.paused?'渠道已恢复':'渠道已暂停');await loadRoute();}catch(e){toast(e.message);}};document.querySelectorAll('tbody tr').forEach((tr,i)=>{const o=s.orders?.[i];if(o&&['SUBMITTED','PARTIAL_FILLED'].includes(o.status))tr.onclick=()=>cancelOrder(o,b);});}
async function cancelOrder(order,binding){if(!confirm(`确认申请撤销Futu订单？\n订单 ${order.channel_order_id}\n${order.side} ${order.quantity}股\n策略 ${binding.release_id||'未绑定'}`))return;try{const {token}=await post('/api/channels/futu/cancel-token',{channel_order_id:order.channel_order_id});if(!confirm('撤单令牌已签发，有效期2分钟。再次确认撤单。'))return;await post('/api/channels/futu/cancel',{channel_order_id:order.channel_order_id,token});toast('撤单请求已提交');await loadRoute();}catch(e){toast(e.message);}}

function renderComparison(s){document.querySelector('#app').innerHTML=`<section class="panel hero"><div><div class="eyebrow">只读评估</div><h1>虚拟账户比较</h1><div class="subtle">共同观察区间 ${esc(s.common_window?.start)} 至 ${esc(s.common_window?.end)}</div></div></section><section class="panel section"><div class="section-head"><h2>参与账户</h2><span class="badge">无交易干预</span></div><div class="comparison-select">${state.accounts.map(a=>`<label><input type="checkbox" value="${esc(a.account_id)}" ${s.accounts.some(x=>x.account_id===a.account_id)?'checked':''}> ${esc(a.name)} · ${esc(a.release_id)}</label>`).join('')}</div></section><section class="panel section"><div class="section-head"><h2>核心指标</h2><span class="badge">OPC优先级</span></div>${rowsTable([['账户',r=>r.account_id],['策略发布',r=>r.release_id],['累计收益',r=>pct(r.metrics.total_return)],['最大回撤',r=>pct(r.metrics.maximum_drawdown)],['卡玛比率',r=>r.metrics.calmar_ratio==null?'不可用':Number(r.metrics.calmar_ratio).toFixed(3)],['盈亏比',r=>r.metrics.win_loss_ratio==null?'不可用':Number(r.metrics.win_loss_ratio).toFixed(3)]],s.accounts)}</section>`;document.querySelectorAll('.comparison-select input').forEach(el=>el.onchange=()=>{const ids=[...document.querySelectorAll('.comparison-select input:checked')].map(x=>x.value);navigate(`/comparison${ids.length?'?'+comparisonQuery(ids):''}`);});}

async function loadAccounts(){const payload=await getJson('/api/virtual-accounts');state.accounts=payload.accounts||[];return payload;}
async function loadRoute(){const route=parseRoute(location.pathname);setNav(route.page);try{const accountIndex=await loadAccounts();if(route.page==='account'&&!route.accountId){const preferred=localStorage.getItem('pte.lastAccountId');const valid=state.accounts.some(a=>a.account_id===preferred);const id=valid?preferred:accountIndex.default_account_id;if(id){navigate(`/accounts/${encodeURIComponent(id)}`,true);return;}throw new Error('尚无虚拟账户');}const scope=route.page==='account'?route.accountId:route.page;const request=state.loader.begin(scope);document.querySelector('#app').innerHTML='<div class="loading">正在加载当前作用域…</div>';let snapshot;if(route.page==='account')snapshot=await getJson(`/api/virtual-accounts/${encodeURIComponent(route.accountId)}/snapshot`,{signal:request.signal});else if(route.page==='channel')snapshot=await getJson('/api/channels/futu/snapshot',{signal:request.signal});else{const ids=new URLSearchParams(location.search).getAll('account_id');snapshot=await getJson(`/api/comparison${ids.length?'?'+comparisonQuery(ids):''}`,{signal:request.signal});}if(!state.loader.accept(request,scope))return;if(route.page==='account'&&snapshot.scope.account_id!==route.accountId)throw new Error('服务端账户作用域不一致');state.lastSuccess=new Date();if(route.page==='account'){localStorage.setItem('pte.lastAccountId',route.accountId);renderAccount(snapshot);}else if(route.page==='channel')renderChannel(snapshot);else renderComparison(snapshot);}catch(error){if(error.name==='AbortError')return;document.querySelector('#app').innerHTML=`<div class="error"><strong>当前作用域加载失败</strong><div>${esc(error.message)}</div><div class="subtle">最后成功 ${state.lastSuccess?state.lastSuccess.toLocaleString():'无'}</div></div>`;}}
function navigate(url,replace=false){history[replace?'replaceState':'pushState']({},'',url);loadRoute();}
function boot(){document.querySelectorAll('a[href^="/"]').forEach(a=>a.onclick=e=>{e.preventDefault();navigate(a.getAttribute('href'));});window.addEventListener('popstate',loadRoute);refreshSystem();loadRoute();setInterval(refreshSystem,10000);setInterval(loadRoute,5000);}
if(typeof document!=='undefined')boot();
