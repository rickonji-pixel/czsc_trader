# S004 EX32 设计

- `fund_nav`和`fund_share`可由现有5000积分调用；分别核对覆盖、重复、公告日期和可用时点字段；
- `etf_share_size`官方说明要求8000积分，且上一交易日数据约于次日8:30更新；
- 同日20:30信号必须能证明该交易日数据在20:30前已发布，只有日期或次日发布均不通过；
- 协议冻结前做过字段与访问探测并观察到公告日期结构，因此本轮仅作为数据可用性审计，
  不构成参数、收益或机制证据；
- 若失败，禁止使用事后净值回测同日折溢价。延迟份额研究须另立假设并至少滞后至T+2开盘。
