# S007 EX29 协议

- 审计对象：EX28选出的`S007-EX27-DECOUPLED-ENTRY-BOUNDARY-02821`；
- 数据截止：`2026-09-02`，不得读取截止日后的收益；
- PBO样本：EX27全部合格试验按交易行为去重后的收益路径；
- DSR有效试验数：由上述收益矩阵的相关结构计算；
- DSR原始试验数：七轮正式搜索的全部提案，加EX12消融、EX16已完成局部扰动和EX25配对消融，作为保守搜索次数上界；
- Bootstrap：固定10,000次，平均区块长度10、21、42个交易日；21日为主口径；
- BuyHold：同一交易日历、初始权益1.0、首日开盘买入、单边10bp成本并持有至期末；
- PBO：不高于40%为`FAVORABLE`，高于50%为`ADVERSE`，中间为`MIXED`；
- 有效DSR概率：不低于90%为`FAVORABLE`，低于50%为`ADVERSE`，中间为`MIXED`；
- 绝对Bootstrap：21日区块年化收益90%下界大于0为`FAVORABLE`，仅上界大于0为`MIXED`，否则为`ADVERSE`；
- 相对Bootstrap：21日区块下，年化、最大回撤、卡玛三项胜出概率均高于50%为`FAVORABLE`，均低于50%为`ADVERSE`，否则为`MIXED`；
- 参数邻域：沿用EX28冻结证据；局部可行率不低于80%、最差绩效百分位不低于50%、中心距离不高于0.20为`FAVORABLE`；
- 总标签：出现任一`ADVERSE`即为`ADVERSE`；全部方向均为`FAVORABLE`才为`FAVORABLE`；其余为`MIXED`。

PBO只评价最终同构搜索族，避免把不同原型的收益矩阵强行混合。DSR使用完整研发搜索次数上界施加更保守的多重试验惩罚。
