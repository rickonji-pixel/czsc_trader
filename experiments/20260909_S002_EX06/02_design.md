# 20260909_S002_EX06 设计

## 冻结输入

- 原型来源：已归档的`20260909_S002_EX05`；
- 标的与开发截止：`510500.SH`，`2026-09-08`；
- 唯一入场：日线`bar_triple_V230506::三连跌`由其他状态切入；
- 风险因子：日线`tas_macd_bs1_V230411::顶背驰`与30分钟
  `cxt_overlap_V240612::压力`；
- 后复权行情计算信号，不复权行情用于交易；收盘决策，次日开盘成交；
- 100%目标仓位、最多持有5个完整交易日、单边费用0.05%、初始资金100万元。

## 2×2归因矩阵

| 原型 | 顶背驰过滤 | 压力过滤 |
| --- | --- | --- |
| `P-A00-NO-FILTER` | 关闭 | 关闭 |
| `P-A10-TOP-DIVERGENCE` | 开启 | 关闭 |
| `P-A01-STRUCTURE-PRESSURE` | 关闭 | 开启 |
| `P-A11-BOTH-FILTERS` | 开启 | 开启 |

过滤开启时，该状态既阻止新入场，也请求退出已有持仓。风险退出优先于持有期退出，持仓
期间忽略重复入场，退出后必须等待新的入场状态切换。

## 回放与归因

独立回放2021至2025各自然年、2026YTD和2021至2026YTD连续窗口。报告最大回撤、卡玛
比率、盈亏比、收益率、夏普、闭合交易数和暴露率，并计算2×2矩阵的顶背驰主效应、压力
主效应和交互效应。这里的效应是保持其他规则不变后的路径差值，用于解释系统行为。

同时逐日比较四个版本的动作，列出哪些入场被阻止、哪些持仓被提前退出，以及这些干预
是否集中在少数日期。指标差值和干预次数均为诊断证据，不构成自动准入或淘汰线。

## 审计边界

重新生成三条信号，并校验目标状态切入日期与EX02事件档案一致。EX05双过滤版本必须在
每日目标仓位、订单和分窗口指标上精确复现。本轮不调用SE排名、SM或PTE。
