# S008 EX23 实验设计

1. EX22的`EXECUTION_ERROR`及其证据保持不变；
2. 运行时相对EX22只增加`entry.limit_parameter=0.0`，表示不在次日开盘参考价上附加偏移；
3. 保留EX22已经补齐的`exit.limit_ratio=0.1`；
4. 继续复用EX20冻结的实验逻辑和普通受管临时目录；
5. 禁止修改组件、三套原型、参数锚点、成本、仓位、数据或评价口径。

