# S003 EX31 设计

仅查询EX30无法解释缺失涉及的股票基础信息。若缺失交易日大于等于该股票明确的退市日期，
标记为`DELISTED_STALE_MEMBERSHIP`；原有停牌仍标记为`SUSPENDED`，正常行情标记为`OBSERVED`。

退市标记只解释月度权重快照的更新滞后：不填收益、不以前后价格替代，也不使用后来纳入的
替代成分回填历史。全部13条均能被退市日期解释且没有剩余缺失，数据门方可通过。
