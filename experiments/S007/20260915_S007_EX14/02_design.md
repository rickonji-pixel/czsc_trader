# S007 EX14 设计

EX09—EX13保持为不可变历史证据。本轮沿用EX08已经冻结的七个因子、方向、STC模板、参数
边界、Optuna NSGA-II方法、1200次预算和全部业务门槛，只将搜索环境的单边比例成本从5bp
前瞻性调整为10bp。10bp不进入Optuna搜索空间，所有试验使用同一成本；账户从单位净值1.0
开始，不使用绝对金额，也不包含富途15元固定平台费。

本轮只产生参数搜索证据。存在合格试验时进入新参数平台审计，不选择冠军、不创建候选。
