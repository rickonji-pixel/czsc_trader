# S008 EX28 实验结论

机器裁决：`EXECUTION_ERROR`。根因是汇率跨市场日历与 SRT `PREVIOUS_SESSION`精确截止语义不匹配，
不构成金融机制、原型或参数空间的负面证据。

后继实验应把汇率输入改为最多允许7个自然日陈旧的`LATEST_AVAILABLE`，同时保留组件计算中的
`shift(1)`；必须以13组件与EX16受管面板逐值一致证明因果语义没有改变，然后才能启动搜索。
