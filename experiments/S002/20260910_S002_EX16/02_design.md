# 20260910_S002_EX16 设计

状态：FROZEN。

## 顺序关口

`研究候选登记 → BuyHold PK → 冻结前体检 → 人工确认 → SM冻结 → PTE`

体检要求研究候选登记、PK、证据完整性、可复现性、正式执行回放、成本压力和监测方案
全部通过。机制、统计、参数稳健性、横截面和执行证据允许标记为 `MIXED` 并完整披露；
任一方向为 `WEAK` 或存在阻断项时不得建议冻结。

## 输入证据

- EX09：统计稳健性、参数邻域、逐年留一与成本压力；
- EX11：512100横截面验证和510500补充诊断；
- EX12：正式研究候选登记；
- EX14：项目正式执行规则与SE独立回放；
- EX15：对BuyHold的明确PK；
- `S002-C001_MONITORING.md`：冻结后观测及失效复核规则。

本轮只允许输出 `RECOMMEND_FREEZE`、`KEEP_RESEARCHING` 或
`INSUFFICIENT_EVIDENCE`。即使建议冻结，也必须经过人工确认。

