# S007 EX39 目标

在不修改 `S007-v1` 公式、参数、成本和执行规则的前提下，对正式账户139笔闭合交易执行固定策略收益归因，回答三个问题：

1. 收益、亏损和短周期往返主要出现在哪些入场前可见的市场状态；
2. 入场基础分与确认分强度能否区分短交易、持续交易和盈利交易；
3. 下一候选应优先研究分级仓位、退出状态机还是策略适用范围。

本实验只生成诊断证据，不创建候选，不修改冻结版本，不改变PTE状态，也不把诊断指标升级为新硬门。
