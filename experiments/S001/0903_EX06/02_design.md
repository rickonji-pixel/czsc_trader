# 0903_EX06 设计

## 冻结输入

- 候选池：`0903_EX05/candidate_manifest.json`完整1,187项候选；
- 在位策略：`S001-v1`；
- 临时冠军由 OPC-v3 确定性阶段重新产生，不在协议中预指定；
- 数据截止：2026-09-02；
- 执行规则：沿用候选payload内冻结的唯一执行语义；
- 确定性标准：沿用 OPC-v2 的非劣、目标改善和 Pareto 排名；
- 审计协议：`champion-audit-v1`，随机种子20260903。

## 完整体检

临时冠军由 SE 统一执行：

1. 执行因果、精确重算、试验账本和身份审计；
2. 双倍费率和实际成交价15/30/50bp不利滑点压力；
3. 10区块、252互补切分的标准Sharpe-PBO；
4. 原始试验数和收益相关有效维度两种DSR；
5. 冠军对在位策略及Pareto第一层其他候选的配对平稳区块Bootstrap；
6. 最近20个行为去重真实候选的Range权重L1邻域检查。

Bootstrap固定10,000次，平均区块长度21为主结果，10和42为敏感性结果。统计结果生成
`FAVORABLE`、`MIXED`或`WEAK`风险标签，不设置数值自动否决线。证据缺失或身份不一致
返回`INSUFFICIENT_EVIDENCE`；工程审计失败返回`KEEP_INCUMBENT`。

## 裁决边界

`RECOMMEND_FREEZE`只表示确定性规则通过、工程体检通过且统计证据完整。统计风险仍需人工
阅读和确认。EX06不会调用SM或PTE，也不会创建影子账户。
