# S004 EX47 设计

- 固定观察口径见 `artifacts/protocol.json`；
- `run_probe.py` 每次只查询上一交易日的 588080.SH，不读取未来收益；
- 查询结果写入未跟踪的 `.tmp/s004-margin-arrival/observations/`，避免未完成证据污染正式实验档案；
- 非交易日或不在 08:30—09:15 的调用只算预演，不计入正式 5 日样本；
- 收齐样本后再固化证据、生成清单并关闭实验。
