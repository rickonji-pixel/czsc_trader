# S007 EX24 设计

- 固定EX22的七个因子、方向、因果归一化方法、`STC-T02-GATED-SCORE`原型和九参数联合搜索结构；
- 只扩展EX23证据指向的三个边界：`confirmation_total` 0.20—0.45 改为 0.10—0.45，`entry_timing_total` 0.05—0.25 改为 0.05—0.35，`exit_quantile` 0.20—0.55 改为 0.20—0.65；
- 其余六个参数边界、风险剩余权重、单因子权重和分位差约束保持不变；
- 使用Optuna NSGA-II、InMemory Storage、固定新种子、3000次试验和8个工作线程；
- 沿用EX22的10bp单边成本、单位净值、三目标和全部硬门；
- 搜索完成后，对新ledger执行与EX23同口径的九维L∞参数平台和5%边界带审计；
- 平台准入标准保持为至少10种独立合格交易行为，最大连通区域至少覆盖5种行为；
- 边界依赖标准保持为最大平台中至少50%参数点落在同一侧5%边界带；
- 本轮只回答扩边是否解除人为截断，不因为最优单点提升而直接形成候选。
