# S007 EX31 设计

候选身份冻结七个因子方向、五个基础分权重、两个确认门权重、三个阈值、252日因果分位归一化和
单边10bp成本。TDR从EX04不可变因果特征面板重建决策，并以20%涨跌停边界的可成交限价在次日
开盘重放整手账户；该限价只实现研究阶段“次日开盘成交”的正式执行语义，不改变策略信号。

SE只有在来源哈希、候选哈希、完整目标序列、TDR账本审计及最低样本全部通过时，才可建议登记
研究候选。统计证据和跨标的证据均保留`MIXED`风险。本轮不创建策略版本，不修改SM或PTE。
