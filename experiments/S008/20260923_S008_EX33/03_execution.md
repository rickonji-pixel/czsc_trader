# S008 EX33 执行记录

状态：`EXECUTION_ERROR`。DFLS准备和 CPI 组件物化均成功，但搜索前 EX16 逐值等价门在
`price_return_5d`空值掩码处停止。只读差异审计确认：12个组件只在2024-12-31差一行，因为 SRT
的最后交易日使用2024-12-30信号；宏观实际现金代理另在开发池前25个交易日不同，因为 EX16 的
CPI 原始口径从2013-07-31开始，不带入开发首日前的 CPI 状态，而 EX33 带入了旧状态。

错误发生在 BuyHold和Optuna之前。没有计算目标收益；三套原型均为0/1,500 trial，8逻辑核心
搜索未启动。
