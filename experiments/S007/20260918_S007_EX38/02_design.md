# S007 EX38 设计

- 保持 S007-v1 的因子、权重、阈值、目标仓位算法和开发截止日不变；
- 将冻结规则中的 `virtual_fill.sell=marketable_limit_at_open` 统一解释为渠道无关的 MARKET 卖单，显式限价退出仍保持 LIMIT；
- 研究截止日前读取哈希固定的冻结因子证据，截止日后才拼接 DFLS 发布的新数据，避免重算浮点误差改变历史排名与目标仓位；
- 使用 SRT Runner 与 TDR BacktestChannel 重放完整开发窗口，由 SE 独立检查订单成交因果、账户账本、交易配对和指标；
- 逐日比较当前目标仓位与 EX31 冻结证据，并精确比较正式回放指标、闭合交易数和行为哈希；
- 验证 EX31、EX34、EX35、EX37 历史档案的完整性及来源哈希，不覆盖原实验；
- 不重跑参数搜索，不修改 PTE 运行状态。
