# S008 EX37 执行记录

状态：`EXECUTION_ERROR`。统一评价起点计算为2014-07-30，组件与信号等价门继续通过；但
`base_instance.run_window()`仍按实例自身2013-07-29至2024-12-31的完整开发窗口遍历，执行器的
`evaluation_start`不会改变 SRT 实例交易日期，所以第一步仍在上市日前信号日快照处缺少参考
收盘价并停止。

错误发生在账户权益等价、BuyHold和Optuna之前。没有计算目标收益；三套原型均为0/1,500 trial，
8逻辑核心搜索未启动。
